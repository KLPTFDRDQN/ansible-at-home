---
- name: Install multiple packages
  hosts: all
  become: yes  # Run as sudo
  tasks:
    - name: Execute multiple commands
  command: "{{ item }}"
  loop:
    - ls -l > /tmp/result.txt
    - date >> /tmp/result.txt
    - echo finished >> /tmp/result.txt
  register: <result_variable>
