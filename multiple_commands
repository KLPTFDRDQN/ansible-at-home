---
- name: Install multiple packages
  hosts: all
  become: yes  # Run as sudo
  tasks:
    - name: "Create some files"
      ansible.builtin.file:
        state: touch
        path: /tmp/{{ item }}
     loop:
       - example_file1
       - example_file2
       - example_file3
